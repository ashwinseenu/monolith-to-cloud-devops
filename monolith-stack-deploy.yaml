template-file-path: monolith-stack.yaml
parameters:
  # Uses 'ssm' the value at deployment time, aws wont pass ssm values in UserData Section, if required , configure aws cli in instance by setting account ID, ssm path, required policy for EC2 to access secrets, and get from environment.
  KeyName: "newpem"

  DBMasterPassword: "admin.3210."

  # Non-sensitive items
  GitRepoUrl: "https://github.com/ashwinseenu/monolith-to-cloud-devops.git"
  DBMasterUser: "admin"
  InstanceType: "t2.micro" 
  DomainName: "ashwinseenu.online"

tags:

  Environment: "dev"






